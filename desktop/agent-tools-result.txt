
=== ① 工具清单（P6-09）===
  [PASS] 7 个只读工具
  [PASS] **全部是 read 权限**
  [PASS] 每个都有超时与输出限额
  [PASS] **没有写/执行工具**

=== ② workspace 由 openProject 设定 ===
  [PASS] workspace = 打开的项目
  [PASS] **Agent 侧没有 setWorkspace 入口**

=== ③ 在真实数据上跑七个工具 ===
  [PASS] readFile 读到真实文件
  [PASS] listDir 列出真实目录
  [PASS] search 在真实代码里搜到
  [PASS] symbols 可调用（索引不存在时也明确说明）
      symbols 结果：{"symbols":[{"name":"safe_byte","kind":"Sym","package":"mbp/platform/http","path":"\\http\\http.mbt","line":28,"col":8,"
  [PASS] getProjectInfo 读真实项目类型
  [PASS] getRunLog 可调用（无运行时给出状态）
  [PASS] getDiagnostics 可调用（读渲染侧统一问题模型）

=== ④ 越界仍然被拒（沙箱在真实路径下也生效）===
  [PASS] `../secret.txt` → 拒
  [PASS] 绝对路径在 workspace 外 → 拒
  [PASS] listDir 越界 → 拒
  [PASS] 文件不存在 → ok:false（不抛）

=== ⑤ 关闭项目后 workspace 清空 ===
  [PASS] 关闭后 workspace 清空（不再是刚才那个项目）
  [PASS] **关闭项目后文件类工具应全部被拒**（不默默退到某个目录）

结果：19 通过 / 0 失败 / 共 19 项
