import {
  "mbp/platform/http",
  "mbp/platform/http/server",
  "mbp/platform/pg/client" @pgclient,
  "mbp/platform/redis/client" @redisclient,
  "moonbitlang/async",
}

import {
  "mbp/platform/http/client" @httpclient,
  "moonbitlang/async/socket",
  "moonbitlang/core/encoding/utf8",
} for "test"

supported_targets = "native"

// `async test` 需要 import `moonbitlang/async`（编译器误报未使用）。

warnings = "-29"
